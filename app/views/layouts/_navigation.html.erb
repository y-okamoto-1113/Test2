<nav class="navbar-default navbar-static-side" role="navigation" data-turbolinks="false">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <!--
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="clear">
                            <span class="block m-t-xs">
                                <strong class="font-bold"></strong>
                            </span> <span class="text-muted text-xs block"><%= current_admin.name %> <b class="caret"></b></span>
                        </span>
                    </a>
                    -->
                    <strong class='nav-bar-logo-btn'> <%= link_to current_admin.name, root_path %> </strong>
                    <ul class="dropdown-menu animated fadeInRight m-t-xs">
                        <li> <%= link_to 'ユーザー一覧', root_path, method: :get %> </li>
                        <li> <%= link_to 'ログアウト', destroy_admin_session_path, method: :delete %> </li>
                    </ul>
                </div>
                <div class="logo-element fa fa-chevron-up" >
                    IN+
                </div>
            </li>
            <li class="<%= 'active' if current_page?('/') %>">
                <%= link_to "/" do %>
                <i class="fa fa-laptop"></i> <span class="nav-label fa fa-chevron-up">社員一覧</span>
                <% end %>
            </li>
            <!--
            <li class="<%= 'active' if current_page?('/home/minor') %>">
                <%= link_to "/home/minor" do %>
                <i class="fa fa-desktop"></i> <span class="nav-label">アカウント管理</span>
                <% end %>
                -->
            </li>
        </ul>

    </div>
</nav>
